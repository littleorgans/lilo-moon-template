---
"@littleorgans/create-app": patch
---

`create-app` classifies this repository's new nightly `published-shape.yml` workflow as the
template's own, so a generated project gets no copy of it. Generated projects are unchanged.
