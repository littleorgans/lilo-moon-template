---
"@littleorgans/create-app": patch
---

A generated project's `AGENTS.md` names the full `lilo/build` skill set, and the `lilo/build-core`
bundle that selects it, instead of `lilo/build/start-project` alone.
