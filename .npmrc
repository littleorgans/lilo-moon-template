# Fail fast rather than silently resolving a peer the project never declared.
auto-install-peers=false
strict-peer-dependencies=true

# Keep node_modules honest: a package may only import what it declares.
resolution-mode=highest
