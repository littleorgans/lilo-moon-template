language: "typescript"
layer: "application"
tags: ["web-app"]
dependsOn:
  - "auth"
  - "theme"
  - "ui"
  - "views"
  - "auth-tanstack"
  - "db"

tasks:
  preview:
    # Match vite.config.ts so dev and preview use the same OAuth callback origin.
    env:
      PORT: "5199"
