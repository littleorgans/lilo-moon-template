packages:
  - "apps/*"
  - "packages/*"
  - "services/*"

# lefthook's npm postinstall is unused. Root prepare runs lefthook install.
# pnpm 11 fails the install unless this package is listed here as a boolean.
allowBuilds:
  lefthook: false

# One version of every shared dependency, for the whole repo.
# Projects reference these with "catalog:" and never pin a version themselves.
catalog:
  "@types/node": "^26.2.0"
  "@types/react": "^19.2.18"
  "@types/react-dom": "^19.2.4"
  "@vitejs/plugin-react": "^6.1.0"
  "@vitest/coverage-v8": "^4.1.11"
  react: "^19.2.8"
  react-dom: "^19.2.8"
  typescript: "~7.0.2"
  vite: "^8.2.1"
  vitest: "^4.1.11"

# Libraries declare a permissive React peer range rather than the app pin.
catalogs:
  react-peer:
    react: ">=19"
    react-dom: ">=19"

minimumReleaseAgeExclude:
  - "@vitejs/plugin-react@6.1.0"
